# Development-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# or
#   pip install -e ".[dev]"

pytest>=7.0,<9
pytest-cov>=4.0,<6
ruff>=0.4,<1
pre-commit>=3,<5
mypy>=1.6,<2
